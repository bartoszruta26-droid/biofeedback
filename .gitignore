```
# CMake build system
build/
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
Makefile
compile_commands.json
CTestTestfile.cmake

# Qt autogen files
*_autogen/

# Compiled objects and executables
*.o
*.obj
*.a
*.so
*.dll
*.exe

# Core application binary
biofeedback
qt5_mandatory_example

# Configuration files (if they contain local settings)
config/config.json

# Data files (if they are generated/output rather than source)
data/outlines/*.json

```